# Education Landing Page

Static implementation of the supplied Figma landing page.

Run locally on port `30007`:

```bash
python3 -m http.server 30007
```

Then open:

```text
http://127.0.0.1:30007/
```
